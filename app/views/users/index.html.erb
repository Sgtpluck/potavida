HERE ARE ALL THE USERS
<ol>
<% @users.each do |user| %>
  <li><%= user.name %>, <%= user.email %>, <%= user.role %>
    <%= link_to 'Change User Role', change_user_role_path(user), class: "btn btn-large btn-primary" %> 
   <!--  <#%= link_to 'Destroy User', destroy_user_path, class: "btn btn-large btn-primary" %>
     -->
  </li>
  <% end %>
</ol>